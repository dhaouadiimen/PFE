/* import storage from 'redux-persist/lib/storage';
import {PURGE} from 'redux-persist';
import { Refresh } from '@material-ui/icons';
export const Refresh_Discu = 'Refresh_Discu';

export const AutoRefreshDiscussion = (discussion) => ({
  type: Refresh_Discu,
  payload: {discussion},
}); */
"use strict";